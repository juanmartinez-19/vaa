<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.VanAlAeropuerto" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Dark variant of the primary brand color -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- Color of the navigation elements -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Texto y íconos de la Toolbar -->
        <item name="android:colorPrimary">@android:color/white</item>
    </style>

        <!-- Define un estilo para los elementos del Spinner -->
        <style name="SpinnerItemStyle">
            <item name="android:textSize">18sp</item> <!-- Ajusta el tamaño de fuente -->
            <item name="android:padding">8dp</item> <!-- Ajusta el padding -->
        </style>

</resources>

